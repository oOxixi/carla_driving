# Model Directory

This directory stores local model weights.

Large models are not included in GitHub.

正式比赛只使用 Qwen 2B，权重由服务器独立管理，不提交到本仓库。
推荐结构：

models/
└── Qwen-2B/

Example:

MODEL_PATH="models/Qwen-2B"

历史大模型不再保留为可选正式配置。

