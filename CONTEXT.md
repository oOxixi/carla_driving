# 项目领域与阅读入口

本工作树承载 CARLA 多模态驾驶基础链与 J6P 挑战赛道。正式车辆入口为
`python -m integration.carla_runner`。完整三级导航见
[项目架构总览](docs/architecture/README.md)。

## 统一术语

| 术语 | 本项目中的含义 |
|---|---|
| 驾驶指令 | 语音/NLU 或场景注入的用户意图；不等于已授权控制 |
| ModelRequest V1 | 送入高层规划器的结构化请求；Schema 是交换契约 |
| ManeuverPlan V2 | 1–4 步高层机动序列；经校验、编译与执行反馈闭环 |
| Teacher | 数据采集/对比实验所固定的 Qwen 代码、模型 revision 与权重身份；不是所有历史 Qwen 的统称 |
| Student | 四路固定 Shape 输入、十个结构化输出 Head 的轻量规划网络 |
| A 状态机 | 指令生命周期与机动执行；不要与挑战赛道 A1–A4 人员编号混淆 |
| B/C/D 控制 | B 横向、C 纵向、D 最终安全仲裁；接口文档旧角色 B 的规划职责不是横向控制类的别名 |
| 模块（Module） | 有明确接口（Interface）与实现（Implementation）的功能集合 |
| 接口（Interface） | 字段、单位、时序、配置、异常与使用前提的整体；不只函数签名 |
| 替换位置（Seam） | 允许 Teacher/Student 或不同 Runtime Adapter 替换的接口位置 |
| Adapter | 完成明确输入输出转换的实现；转换不自动证明语义等价 |
| 证据/Gate | 指定模型、数据、配置、代码和环境下的验证记录/准入结论 |
| 模型就绪 | 某后端的局部状态；不自动等于独立评测或 J6P 最终通过 |

## 不变量

- 高层规划不得输出 `steer/throttle/brake`，最终执行要经过 D；评分不得反向改变控制事实。
- 感知必须记录来源；world/virtual 和场景真值不能冒充 sensors 结果。
- 静态检查、离线测试、CARLA 闭环、X86 和 J6P 证据分别报告。
- 冻结数据、历史模型和旧报告不因当前版本变动而被追溯改写。
- 每项实验的代码、权重、数据、配置与评测身份必须能关联；缺口见审计台账。

本页记录当前理解，不新增未经验证的硬件能力或替代现有 Schema。
