# 第二组实施与验收总结（2026-07-31）

## 1. 结论先行

本次已在 `/home/abc/projects/carla_driving_main_20260730` 完成第二组 A/B/C/D 的接口、
快慢路由、Qwen 服务边界、三传感器感知、安全控制、CARLA 闭环、自动测试和证据索引
建设，并修复 Town10HD 20 km/h 出道路、watchdog 误锁存、传感器预热、完整感知计时和
安全冲突命令无终态等问题。

**总体验收状态：PARTIAL / 未达到正式封板条件。** 不能标记为全量 PASS，原因有四项：

1. 当前主机没有 Qwen2.5-VL 7B、3B 或 AWQ/INT4 权重，无法完成本机真实模型对比；
   历史 7B P95 为 `2478.419 ms`，明显高于 `300 ms` 目标。
2. Town03 真实 RGB/LiDAR/Radar 感知采集 P95 为 `40.099–70.650 ms`，高于 C 的
   `30 ms` 目标；S01、D03 活跃管线 P95 分别为 `70.991 ms`、`69.323 ms`，未达到
   20 Hz 所需的 `≤50 ms` 周期。
3. 原方案阶段 4 要求 S01、D03、D08 各不少于 5 个 seed、20 次正式回归及 60 分钟
   稳定性实跑；本次只做了当前修改后的代表性单 seed 回归，阶段门禁未满足后未继续
   生成形式上完整但不能验收的矩阵。历史矩阵和历史 30 分钟日志只登记为
   `HISTORICAL_PRE_CHANGE`。
4. 当前真实传感器日志明确记录 `RGB_DETECTOR_UNAVAILABLE`，所以 RGB 流存在且同帧，
   但没有固定 ONNX 检测器参与目标语义输出；当前目标距离主要来自 LiDAR，速度来自
   Radar/LiDAR 关联。

在当前代表性通过运行中，S01、D03、D08 和 Town10HD 20 km/h 回归均为 0 碰撞、
0 闯红灯、0 严重路线偏离；D 的纯控制与安全仲裁 P95 为 `0.0294 ms`，满足 `≤5 ms`。

## 2. 验收口径和证据诚信

本报告依据原始文件
`/home/abc/Downloads/第二组四人内部任务分工与验收方案.docx` 逐项核对，采用以下口径：

- 合成感知基准只证明 Python 同步、跟踪和融合逻辑，不作为实时 CARLA/ONNX 延迟；
- 确定性 Qwen 测试后端只证明接口和错误处理，不作为真实模型正确率或延迟；
- 历史 Qwen、场景矩阵和 30 分钟稳定性只作为修改前背景，不作为当前代码验收；
- 缺少权重、视频、正式矩阵或 60 分钟运行时直接标 `BLOCKED/NOT_RUN`，不补造数据；
- CARLA 的 `sensor_to_control_ms` 从“感知已就绪”开始，不能代表完整感知链；本次新增
  `perception_acquire_ms` 和 `pipeline_active_ms`，以真实完整口径判定性能。

机器可读总表位于：

- `artifacts/second_group_20260731/live_carla_acceptance_summary.json`
- `submission/second_group_20260731/evidence_index.json`
- `artifacts/second_group_20260731/environment_snapshot.json`

## 3. 环境与代码基线

| 项目 | 当前值 |
|---|---|
| 仓库 | `https://github.com/oOxixi/carla_driving.git` |
| 分支 | `main` |
| 基线 commit | `b9150fcbda6959fcd6c50269b09b431b750632ba` |
| 工作树 | dirty；保留用户既有实时语音和 artifacts，未 reset/清理 |
| CARLA | `0.9.16` |
| 实测地图 | `Town10HD_Opt`、`Town03_Opt` |
| GPU | RTX 4060 Laptop，8188 MiB |
| 驱动 | `580.173.02` |
| Python 环境 | `conda carla312` |
| Torch | `2.11.0+cu130` |
| ONNX Runtime | `1.27.0` |
| Qwen 权重 | 当前项目、Hugging Face/ModelScope 缓存均未找到 |

证据索引保存每个关键源文件和结果文件的 SHA-256，并保存基线 commit、工作树状态和
tracked diff SHA-256。由于当前改动尚未提交，基线 commit 不能被表述为“包含本次改动的
冻结提交”。

## 4. 成员 A：系统集成与全链路

### 4.1 已实现

1. **六类 V1 接口冻结：PASS**

   - `interfaces/driving_command.schema.json`
   - `interfaces/model_request.schema.json`
   - `interfaces/decision_plan.schema.json`
   - `interfaces/perception_state.schema.json`
   - `interfaces/control_command.schema.json`
   - `interfaces/execution_feedback.schema.json`
   - 每类都有 `interfaces/examples/` 样例；`runtime/interface_registry.py` 使用缓存的严格
     JSON Schema 校验，拒绝未知字段、坏版本、非 JSON 和非有限数。

2. **确定性快路由与异步慢路由：代码与自动测试 PASS，真实慢模型 BLOCKED**

   - `runtime/orchestrator.py` 将 START/STOP/EMERGENCY_STOP/SET_SPEED/SLOW_DOWN/
     KEEP_LANE 放入快路径；复杂、模糊、冲突请求进入私有 Qwen worker。
   - Qwen、结果、传感器和审计队列均有上限；队列溢出时被挤出的旧请求明确返回
     `REJECTED/QUEUE_OVERFLOW`，不会永久悬空。
   - deadline、超时、过期、非法 Schema、低置信度、目标 ID 不存在和迟到结果全部
     fail-closed；一个 command_id 只允许一个终态。
   - `integration/canonical_bridge.py` 把 V1 对象真正接入 CARLA runner。复杂请求等待时
     先停车；Qwen 返回后再次校验 deadline、最新感知、目标 ID 和 D 可执行性。
   - 当前 D 尚不支持的转向、变道、靠边等计划明确拒绝并继续停车，不把模型输出直接
     变成油门、制动或方向盘。

3. **时间戳和证据：PARTIAL**

   - `runtime/latency_trace.py` 支持 audio/VAD/ASR/NLU/perception/Qwen/planning/
     arbitration/action 分段及 mean/P95/P99/max。
   - CARLA 帧日志新增 `simulator_tick_ms`、`perception_acquire_ms`、
     `pipeline_active_ms`、`sensor_to_control_ms`、`decision_ms`。
   - 当前代表性场景命令未携带真实麦克风 audio/VAD/ASR 时间，因此 `69.610 ms` 等
     标准命令 E2E 仅从结构化命令接收到动作下发，不能冒充真实语音端到端。
   - `ScenarioEvidenceRecorder` 仍同步写 JSONL 并 flush；控制先下发再写日志，但尚无
     真实磁盘阻塞压力证明“日志绝不影响下一控制周期”，故日志非阻塞项只判 PARTIAL。

4. **唯一启动、健康检查、清理和 watchdog：PASS（生产模型健康为 BLOCKED）**

   - `run_full_pipeline.sh` 提供 start/check/strict-check/status/run/stop。
   - PID 停止前核对进程命令行，避免误杀；CARLA Actor 反向销毁并恢复同步设置。
   - watchdog 新增显式 pause/resume，只排除 CARLA 同步 tick 和可视化节拍期间仿真冻结
     的外部等待；感知、路由、控制和日志活跃时间仍计入健康截止时间。
   - `--realtime` 改为只睡到 50 ms 周期剩余量，不再在 tick+控制后额外固定睡 50 ms。
   - CARLA tick 时延单独入日志，既避免假安全锁存，也不隐藏引擎卡顿。

5. **一键复现和证据索引：PASS（正式材料缺项另列）**

   - 运行手册：`docs/SECOND_GROUP_RUNBOOK.md`
   - 证据生成器：`tools/generate_second_group_evidence.py`
   - 证据索引：`submission/second_group_20260731/evidence_index.json`
   - pytest 摘要：`artifacts/second_group_20260731/tests/pytest_full_result.json`

### 4.2 A 未完成/受阻

- 当前修改后的正式 5-seed/20-run 场景矩阵：`NOT_RUN_STAGE_GATE_FAILED`；
- 60 分钟真实稳定性：`NOT_RUN_STAGE_GATE_FAILED`；
- 关键视频：`MISSING`；
- 另一名成员独立复现签字：本次无法替代真人验收；
- 完整真实语音逐阶段 P95：本次没有新的麦克风音频样本和校准环境。

**A 角色结论：PARTIAL。** 核心集成和证据机制完成，但正式回归、60 分钟、视频及完整
语音时间戳未满足。

## 5. 成员 B：Qwen 模型与 GPU 部署

### 5.1 已实现

- `qwen_service/` 提供 `/health`、`/infer`、`/metrics`，支持请求 ID、deadline、并发限制、
  错误码和显存字段；无权重时明确 `DEGRADED`，不会假装 production-ready。
- `qwen_service/prompt_and_schema.md` 固定精简场景、Top-K 目标、短 JSON 和最多 48 token，
  DecisionPlan 无底层控制字段。
- `qwen_service/model_benchmark.json`、`latency_report.json`、`model_card.md` 明确区分当前
  缺失和历史数据。
- 服务及 client 的确定性接口、并发、超时、Schema 和错误路径均包含在全量自动测试中。

### 5.2 模型验收结果

| 候选 | 本机权重 | 当前真实基准 | 结论 |
|---|---:|---:|---|
| Qwen2.5-VL-7B-Instruct | 缺失 | 无；历史 P95 `2478.419 ms` | FAIL / 超 300 ms |
| Qwen2.5-VL-3B-Instruct | 缺失 | NOT_RUN | BLOCKED |
| Qwen2.5-VL-7B-AWQ/INT4 | 缺失 | NOT_RUN | BLOCKED |

历史代理集 7B 的 Schema、动作和目标关联记录为 100%，但它来自其他主机、不是官方隐藏
集，且没有可验证的本机权重 SHA-256、许可证、VRAM 或吞吐，不能用于选择生产模型。

**B 角色结论：BLOCKED / 核心性能目标 FAIL。** 服务边界可用，但模型对比、当前权重
哈希、许可证、显存、吞吐和 P95≤300 ms 未完成。

## 6. 成员 C：传感器与多模态感知

### 6.1 已实现

- `integration/carla_perception.py` 和 `perception/sensor_adapter.py` 使用 CARLA frame ID、
  sim time、统一 ego 坐标及固定外参；RGB/LiDAR 为必需同帧流，Radar 为 5 ms 有界的
  可选同帧流。
- Radar 掉线会明确降级，不阻塞 RGB/LiDAR；Radar 径向速度与 LiDAR 距离按 4 m 门限
  关联。
- `perception/rgb_pipeline.py` 提供 ROI、缩放、低频检测/高频跟踪和 Top-K；
  `fusion_tracker.py` 提供稳定 ID、距离、速度、TTC 和风险等级。
- `fault_injection.py/.sh` 覆盖相机黑屏、Radar/LiDAR 掉线、噪声、误报/漏报等降级；
  自动测试校验 invalid/stale/degraded 不伪装为正常。
- `QwenImageStager` 只在控制线程登记同帧 RGB；PNG 编码和落盘在 Qwen 后台线程。

### 6.2 实机探针与时延

Town10HD 低资源三路探针结果：

- 测量窗口：100 个严格同帧帧；
- RGB/LiDAR/Radar 各 103 次有效 callback；
- 三路 invalid callback 均为 0；
- 启动时出现一次预期的 GPU pipeline bubble，随后连续稳定；
- 证据：`artifacts/second_group_20260731/sensor_probe_all_town10hd.log`。

真实 Town03 单 seed 代表性运行：

| 场景 | 感知采集 P95 | 活跃管线 P95 | 感知目标 ≤30 ms | 20 Hz 周期 ≤50 ms |
|---|---:|---:|---:|---:|
| S01 | `70.650 ms` | `70.991 ms` | FAIL | FAIL |
| D03 | `68.918 ms` | `69.323 ms` | FAIL | FAIL |
| D08 | `40.099 ms` | `40.384 ms` | FAIL | PASS |

合成 1000 帧同步/跟踪/融合基准 P95 `0.861 ms` 只证明纯 Python 逻辑，不是实时 CARLA
或 ONNX 延迟；文件已在字段 `not_claimed` 中明确这一点。

S01 冷启动首次运行在第 48 个控制帧出现 `1287.7 ms` tick 之外的感知等待，watchdog
正确 fail-closed，命令 FAILED。完整预热后的复跑通过；首次失败日志保留，用于说明当前
传感器链尚未达到正式稳定门禁。

**C 角色结论：PARTIAL，性能 KPI FAIL。** 同帧、融合、降级、故障注入和探针已实现；
真实 P95 超标，固定 RGB 检测器缺失，尚不能完成“RGB+Radar+LiDAR 全语义融合”验收。

## 7. 成员 D：车辆控制与安全执行

### 7.1 已实现

- `integration/runtime_loop.py` 是 A/B/C/D 的纯 Python 组合点，CARLA runner 只有一个
  `ego.apply_control()`；最终控制必须经过 `SafetySupervisor`。
- 标准速度、启停、减速和保持车道直接进入确定性 FSM，不经过 Qwen。
- 慢路径计划校验 deadline、置信度、目标 ID、最新感知和当前执行能力；非法、过期、
  超时或不支持计划保持停车。
- `safety_supervisor.py` 覆盖 TTC、前距、红灯/停止线、车道/路线偏离、NaN、控制范围和
  油门制动冲突；严重路线偏离禁止恢复油门。
- `execution_feedback.py` 保证最多一个终态；Qwen 队列溢出、迟到结果和运行结束均有明确
  terminal feedback。
- 修复 D08：场景因正确安全接管成功时，仍活跃的冲突命令在帧预算结束得到 `FAILED`
  终态及“安全约束阻止完成”原因，不再留下空终态。

### 7.2 基准与实机场景

纯 D 10000 帧基准：

- P95 `0.029423 ms`，目标 `≤5 ms`：PASS；
- 预期安全接管 `100/100`；
- 油门/制动冲突 `0`；
- 严重安全事件 `0`。

当前修改后的 Town03 代表性运行：

| 场景 | 状态/分数 | 命令终态 | 关键安全结果 |
|---|---|---|---|
| S01 复跑 | SUCCEEDED / 25 | SUCCEEDED | 终速 `17.994 km/h`，0 碰撞/偏离 |
| D03 | SUCCEEDED / 25 | SUCCEEDED | 最小前距 `2.759 m`，前距安全覆盖 380 帧 |
| D08 最终复跑 | SUCCEEDED / 25 | FAILED（预期冲突） | 红灯覆盖 414 帧，0 闯红灯 |

三个场景的 decision P95 分别为 `0.377/0.394/0.310 ms`，均满足 D 的 5 ms 目标。
集成控制频率不达 20 Hz 的主因是感知采集，而不是 D 决策计算；但从系统验收角度，
20–50 Hz 总目标仍为 FAIL，不能只用 D 微基准替代。

**D 角色结论：功能与局部 KPI PASS，正式角色验收 PARTIAL。** 唯一出口、安全规则、
终态和代表性场景通过；完整正式矩阵和系统 20–50 Hz 尚未满足。

## 8. Town10HD 20 km/h 出道路与 watchdog 专项

### 8.1 问题与修复

- 路线投影原先可能在交叉/重叠道路选择几何更近但朝向相反的 waypoint；新增 ego 朝向
  兼容门限和 5 m 空间格网索引。
- 不再每 200 帧无条件重建路线，只在接近末端时续接；候选路线无效时保留旧路线并
  fail-closed。
- 目标落在车辆后方、严重路线偏离或无方向兼容 waypoint 时禁止恢复油门。
- Town10HD 全图 6042 个 waypoint 的索引在控制循环前预热，构建约 `9.90 ms`；局部查询
  不再触发 1.6 秒全图枚举。
- 第二次失败日志显示墙钟跳变约 1.411 秒、仿真只推进 0.05 秒。实测路线构建仅约
  `0.41 ms`，说明不能把它错误归因于路线算法；watchdog 现只排除同步仿真冻结等待，
  CARLA tick 耗时另行记录。

### 8.2 最终复跑

- 1600 帧 / 80 秒仿真；
- 20 km/h 命令终态 SUCCEEDED；
- 最大速度 `5.483 m/s`，最终 `5.338 m/s`；
- 最大路线偏差 `0.778 m`；
- 0 watchdog 帧、0 碰撞、0 严重路线偏离；
- 两次真实交通灯停车后恢复，`RED_LIGHT_STOP_LINE_GUARD` 共 84 帧；
- 场景分数 25/25；CARLA tick 平均 `1.213 ms`、最大 `7.741 ms`。

证据：`artifacts/second_group_20260731/town10hd_speed20_watchdog_fixed/`。

## 9. 自动测试结果

最终命令：

```bash
conda run -n carla312 env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  python -m pytest -q \
  --junitxml=artifacts/second_group_20260731/tests/pytest_full.xml
```

结果：`470 passed, 1 skipped, 0 failed, 0 errors`，耗时约 `2.56 s`。

Ubuntu ROS 自动加载的 `launch_testing` 插件会错误处理 `car_control_B/` 与
`car_control_B2/` 的同名测试，所以固定关闭第三方 pytest 插件自动加载。首次 collection
失败保留在 `pytest_collection_failure.xml`，最终 JUnit 和 JSON 分别为：

- `artifacts/second_group_20260731/tests/pytest_full.xml`
- `artifacts/second_group_20260731/tests/pytest_full_result.json`

## 10. 阶段门禁判定

| 阶段 | 原要求 | 本次状态 | 判定 |
|---|---|---|---|
| 0 接口冻结 | 六类 V1、样例、Schema 测试 | 已完成 | PASS |
| 1 模块独立 | B 服务、C 融合、D 控制、A 路由/时间戳 | 代码/单测完成；真实 B 权重缺失 | PARTIAL |
| 2 首次闭环 | 快路径、基础闭环、E2E≤150 ms、终态 | 结构化命令闭环通过；真实 ASR 分段缺失 | PARTIAL |
| 3 慢路与鲁棒 | 真实异步 Qwen、故障注入、安全降级 | 异步边界/故障测试通过；真实模型未跑 | PARTIAL/BLOCKED |
| 4 正式回归 | 5 seed×20 次、60 min、score/evidence/video | 未运行；传感器/Qwen 门禁失败 | BLOCKED/NOT_RUN |

原文“各≥5 seed×20 次”存在“20 次总计”或“每 seed 20 次”的文字歧义；当前运行手册按
5 seeds × 每 seed 4 次 = 每场景 20 次实现。无论按哪种解释，本次都只跑了代表性 seed 0，
因此不宣称满足正式矩阵。

## 11. 组长九项检查清单

| # | 检查项 | 状态 | 说明 |
|---:|---|---|---|
| 1 | 模型版本与服务固定 | BLOCKED | 服务/接口固定；权重、许可证、SHA-256、真实性能缺失 |
| 2 | 传感器状态可信 | PARTIAL | 同帧/有效性/掉线可信；RGB detector 缺失、P95 超标 |
| 3 | 快路径绕开 Qwen | PASS（代码/测试） | 快路径本地确定性执行；当前单场景实跑未启动真实模型 |
| 4 | 慢路径异步 | PASS（边界）/BLOCKED（实模） | worker、deadline、拒绝齐全；无真实 Qwen 闭环 |
| 5 | 控制出口唯一 | PASS | runner 仅使用 D 后的 final control |
| 6 | 命令 100% 有终态 | PASS（当前最终代表运行） | D08 无终态已修复；首次失败也有 FAILED |
| 7 | 延迟可分段定位 | PARTIAL | CARLA 分段与统计齐全；真实音频/VAD/ASR/Qwen 分段未补 |
| 8 | 安全红线 0 | PASS（当前代表运行） | 0 碰撞、0 闯红灯、0 严重路线偏离；非正式矩阵 |
| 9 | 连续 60 min 真实稳定 | NOT_RUN | 历史仅 30 min，不能升级为 60 min |

## 12. 复现命令

### 12.1 全量自动测试

```bash
cd /home/abc/projects/carla_driving_main_20260730
conda run -n carla312 env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  python -m pytest -q \
  --junitxml=artifacts/second_group_20260731/tests/pytest_full.xml
```

### 12.2 模块基准

```bash
conda run -n carla312 python -m tools.benchmark_perception_pipeline \
  --frames 1000 \
  --output artifacts/second_group_20260731/perception_benchmark.json

conda run -n carla312 python -m tools.benchmark_control_runtime \
  --frames 10000 \
  --output car_control_D/control_benchmark.json
```

### 12.3 三传感器探针

```bash
conda run -n carla312 python tools/check_sensor_stability.py \
  --host 127.0.0.1 --port 2000 \
  --sensor all --profile low --frames 100 \
  --expected-map Town10HD_Opt
```

### 12.4 正式矩阵（门禁修复后才能作为验收）

```bash
conda run -n carla312 python -m tools.run_carla_scenario_matrix \
  --scenario scenarios/smoke/S01_set_speed_20.json \
  --scenario scenarios/safety_D/D03_front_vehicle_brake.json \
  --scenario scenarios/safety_D/D08_command_conflict_red_light_continue.json \
  --seeds 0,1,2,3,4 --repeats-per-seed 4 \
  --output-dir artifacts/second_group_20260731/scenario_matrix
```

### 12.5 证据重建

```bash
conda run -n carla312 python -m tools.generate_second_group_evidence \
  --output submission/second_group_20260731/evidence_index.json
git diff --check
bash -n run_full_pipeline.sh
```

## 13. 后续封板顺序

1. 固定并安装 7B、3B、AWQ/INT4 三类 Qwen 权重，登记许可证和逐文件 SHA-256；在同一
   冻结验证集比较安全正确率、目标关联、VRAM、吞吐和 mean/P95/P99/max。
2. 优化 CARLA 真实感知链：先定位约 40–70 ms 的 GPU callback 等待，再固定 ONNX RGB
   detector、输入尺寸和检测周期；只有真实 P95≤30 ms 后才进入正式矩阵。
3. 将统一证据写入改为有界后台队列，并注入慢磁盘验证控制周期不被日志阻塞。
4. 用断开的真实 Qwen 服务 URL 跑标准指令 CARLA 闭环，生成可审计的 FAST 路由记录和
   快路径命中率；再用真实模型跑复杂命令异步闭环。
5. 完成每场景 5 seeds×4 次（或由负责人澄清原文后采用更严格的 5×20）正式矩阵，
   保留全部失败前后至少 5 秒日志和关键视频。
6. 矩阵全绿且 KPI 达标后再跑完整 60 分钟稳定性，记录 GPU/CPU/显存、队列深度、
   传感器对齐、Qwen 周期请求、故障降级和恢复探针。
7. 提交本次源代码与证据，生成包含本次改动的冻结 commit；由另一名成员按运行手册
   独立复现并签字。

## 14. 最终状态汇总

| 范围 | 状态 |
|---|---|
| 六类接口、严格校验、自动测试 | PASS |
| A 快慢路由、deadline、队列、watchdog、证据 | PASS/PARTIAL |
| B 服务接口 | PASS |
| B 真实模型与 P95≤300 ms | BLOCKED/FAIL |
| C 同帧、Radar/LiDAR 关联、失效降级 | PASS/PARTIAL |
| C 真实感知 P95≤30 ms | FAIL |
| D 仲裁 P95≤5 ms、唯一出口、代表性安全场景 | PASS |
| 系统 20–50 Hz | FAIL（S01/D03） |
| 当前代表运行安全红线 | PASS（非正式矩阵） |
| 正式 5-seed/20-run 矩阵 | NOT_RUN |
| 真实 60 分钟稳定性 | NOT_RUN |
| 当前整体正式验收 | **PARTIAL / NOT READY** |
