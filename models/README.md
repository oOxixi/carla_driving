# Model Directory

This directory stores local model weights.

Large models are not included in GitHub.

Expected structure:

models/
└── Qwen2.5-VL-7B/

Example:

MODEL_PATH="models/Qwen2.5-VL-7B"

The Qwen model weights should be downloaded separately
and placed here before running Day20/Day21 demos.

