# Optional local Qwen2.5-VL environment. Freeze the versions actually used in
# the final model manifest; these packages are intentionally not installed in
# the lightweight CARLA controller environment.
torch>=2.8,<3
torchvision>=0.23,<1
transformers>=4.51,<6
accelerate>=1.5,<2
qwen-vl-utils>=0.0.10,<1
Pillow>=10
