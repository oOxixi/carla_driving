# Model Directory

This directory stores local model weights.

Large models are not included in GitHub.

正式比赛只使用 Qwen3.5-2B，权重由服务器独立管理，不提交到本仓库。
推荐结构：

models/
└── Qwen3.5-2B/

Example:

MODEL_PATH="models/Qwen3.5-2B"

旧的 Qwen2.5-VL 3B/7B 仅属于历史实验，不是当前正式运行依赖。

